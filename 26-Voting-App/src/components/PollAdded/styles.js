import * as v from '../../styles/variables'

const styles = {
  pollLink: {
    padding: '1rem',
    margin: '0.7rem',
    backgroundColor: v.grn.alpha(0.05).string(),
    border: {
      width: 1,
      style: 'solid',
      color: v.grn.alpha(0.2).string(),
      radius: 5
    }
  }
}

export default styles
